/public/
/tmp/

# Go build output
/bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go module cache (CI uses .go/ as GOPATH)
/.go/
/.go-cache/

# Go test/coverage
*.test
*.out
coverage.html

# Local CodeQL databases and SARIF (make codeql-go)
.codeql/

# Minisign secret for blog posts (never commit; elvishsign keygen default name)
content/blog/signing.key

# Local data/secrets (generated by dev scripts)
/data/

# Environment files
.env
.env.local
.env.*.local

# Node (browser bundles — lockfile committed, install with npm ci)
frontend/node_modules/
apps/*/node_modules/
packages/*/node_modules/
ui/**/node_modules/

# Playwright / local test output (root)
/test-results/

# IDE
.idea/
.vscode/*
!.vscode/tasks.json
!.vscode/extensions.json

# Xcode — never commit per-machine UI state or account-derived paths (xcuserdata)
**/xcuserdata/
IOS/Config/Local.xcconfig
*.xcuserstate
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Flutter (generated under flutter/elvish_mail/; see flutter/elvish_mail/.gitignore)
flutter/**/.dart_tool/
flutter/**/build/
flutter/**/.flutter-plugins
flutter/**/.flutter-plugins-dependencies

# Playwright (e2e/)
e2e/node_modules/
e2e/test-results/
e2e/playwright-report/
e2e/.cache/

# Build artifacts from standalone binaries
/elvishmailtest
/elvishrelay

# MkDocs output and staged guide copies (make docs-stage / docs-build)
docs-site/site/
docs/guides/product-readme.md
docs/guides/contributing.md
