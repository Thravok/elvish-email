---
description: E2EE mail pipeline, threat model, and four-store architecture guidance
globs:
  - internal/mail*/**/*.go
  - internal/scyllastore/**/*.go
  - internal/blobstore/**/*.go
  - internal/openpgp/**/*.go
  - internal/keyserver/**/*.go
  - internal/envelope/**/*.go
  - cmd/elvishmailtest/**/*.go
---

# Mail subsystem

Read [docs/e2ee-mail-spec.md](docs/e2ee-mail-spec.md) before making mail changes. It is the source of truth for the encrypted mail product.

## Core invariants

1. **No plaintext body at rest** — after `mailpipe.Ingest*` returns, cleartext is absent from every Cockroach row, Scylla row, blobstore value, log line, and API response. Validated by `internal/mailpipe/no_plaintext_test.go` and `elvishmailtest no-plaintext-audit`.

2. **No server body search** — body search is local-only (browser IndexedDB). CI fails if `internal/httpserver/` mentions `search/body`. See ADR 0008.

3. **Inbound SMTP metadata** — gateway-encrypt paths persist parsed header fields in Scylla for listing/search (ADR 0017). Client-authored mail keeps headers in `header_ciphertext` at ingest.

## Four-store split (ADR 0007)

| Store | Role | Code |
|-------|------|------|
| **CockroachDB** | Identity keys, control tables, outbox state, consent, keyserver cache | `internal/mailmeta`, `internal/db/migrations/` |
| **ScyllaDB** | Mailbox projections, time-ordered access, message events | `internal/scyllastore` |
| **S3-compatible** | Ciphertext blobs (`mail/{uid}/{mid}/body.enc`) | `internal/blobstore` |
| **Valkey** | Sessions, rate limits, keyserver negative cache, SMTP throttle | `internal/session`, `internal/ratelimit` |

## Ingest paths

- **External SMTP** (`mailpipe.IngestExternal`): already-encrypted bodies stored as-is; plaintext gateway-encrypts to recipient pubkey before persistence.
- **Internal client** (`mailpipe.IngestInternal`): rejects cleartext via `openpgp.Sniff` guard; browser encrypts before upload.

## Send modes

| Mode | Description | Code |
|------|-------------|------|
| A — OpenPGP interop | Recipient has published key; ship ciphertext as-is | `internal/mailworker` |
| B — Protected link | AES-GCM body + Argon2id KEK from sender password | `internal/maillinks`, `internal/httpserver/api_protected_links.go` |

## Key files

- `internal/mailpipe/pipe.go` — ingest orchestration
- `internal/mailworker/worker.go` — outbound delivery
- `internal/mailmeta/` — SQL metadata operations
- `libs/go/scyllastore/schema.cql` — Scylla DDL
- `internal/blobstore/s3.go` — blob operations

## Testing

- `make test-mail-e2e` — full stack with Cockroach/Valkey/Scylla/MinIO
- `elvishmailtest` subcommands: `bootstrap-and-selfcheck`, `no-plaintext-audit`, `keyserver-probe`

## Do not

- Log secrets, private keys, plaintext bodies, or session tokens
- Add any `/api/*/mail/search/body` route
- Persist cleartext mail content in any store
- Weaken gateway-encrypt for external mail without an ADR
