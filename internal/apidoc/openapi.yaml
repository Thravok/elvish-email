components:
  securitySchemes:
    elvish_session:
      in: cookie
      name: elvish_session
      type: apiKey
info:
  description: JSON API under /api. Regenerate this file with `make openapi` (see docs/openapi/supplemental.yaml for routes not expressed as literal `p ==` cases in api.go).
  title: ELVish HTTP API
  version: 0.0.0
openapi: 3.0.3
paths:
  /api/admin/auth-captcha:
    get:
      operationId: get_api_admin_auth-captcha
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/admin/auth-captcha
    post:
      operationId: post_api_admin_auth-captcha
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/admin/auth-captcha
  /api/admin/domains:
    get:
      operationId: get_api_admin_domains
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: List mail domains (admin)
    post:
      operationId: post_api_admin_domains
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Add mail domain (admin)
  /api/admin/domains/{domain}:
    delete:
      operationId: delete_api_admin_domains_domain
      parameters:
        - in: path
          name: domain
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Delete mail domain (admin)
    get:
      operationId: get_api_admin_domains_domain
      parameters:
        - in: path
          name: domain
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Get mail domain (admin)
  /api/admin/domains/{domain}/sharing:
    patch:
      operationId: patch_api_admin_domains_domain_sharing
      parameters:
        - in: path
          name: domain
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Update domain sharing mode (admin)
  /api/admin/domains/{domain}/verify:
    post:
      operationId: post_api_admin_domains_domain_verify
      parameters:
        - in: path
          name: domain
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Verify mail domain DNS (admin)
  /api/admin/outbox:
    get:
      operationId: get_api_admin_outbox
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: List outbox entries (admin)
  /api/admin/outbox/{outboxId}:
    get:
      operationId: get_api_admin_outbox_outboxId
      parameters:
        - in: path
          name: outboxId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Get outbox entry (admin)
  /api/admin/outbox/{outboxId}/retry:
    post:
      operationId: post_api_admin_outbox_outboxId_retry
      parameters:
        - in: path
          name: outboxId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Retry outbox delivery (admin)
  /api/admin/outbox/stats:
    get:
      operationId: get_api_admin_outbox_stats
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Mail outbox stats (admin)
  /api/admin/performance:
    get:
      operationId: get_api_admin_performance
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/admin/performance
  /api/admin/performance/export:
    post:
      operationId: post_api_admin_performance_export
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/admin/performance/export
  /api/admin/site/home:
    put:
      operationId: put_api_admin_site_home
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: PUT /api/admin/site/home
  /api/admin/system-mail:
    get:
      operationId: get_api_admin_system-mail
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: System mail config (admin)
  /api/admin/system-mail/preview:
    post:
      operationId: post_api_admin_system-mail_preview
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Preview bulk system mail (admin)
  /api/admin/system-mail/runs:
    get:
      operationId: get_api_admin_system-mail_runs
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: List system mail runs (admin)
  /api/admin/system-mail/send:
    post:
      operationId: post_api_admin_system-mail_send
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Send bulk system mail (admin)
  /api/admin/telemetry:
    get:
      operationId: get_api_admin_telemetry
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/admin/telemetry
    post:
      operationId: post_api_admin_telemetry
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/admin/telemetry
  /api/admin/telemetry/export:
    post:
      operationId: post_api_admin_telemetry_export
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/admin/telemetry/export
  /api/admin/test:
    get:
      operationId: get_api_admin_test
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Test API ping (admin)
  /api/admin/test/auth-posture:
    get:
      operationId: get_api_admin_test_auth-posture
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Auth posture report (admin)
  /api/admin/test/echo:
    post:
      operationId: post_api_admin_test_echo
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Test echo (admin)
  /api/admin/test/health:
    get:
      operationId: get_api_admin_test_health
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Health JSON (admin)
  /api/admin/test/key-material:
    get:
      operationId: get_api_admin_test_key-material
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Key material status (admin)
  /api/admin/test/key-material/dkim/config:
    post:
      operationId: post_api_admin_test_key-material_dkim_config
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Save DKIM config (admin)
  /api/admin/test/key-material/dkim/generate:
    post:
      operationId: post_api_admin_test_key-material_dkim_generate
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Generate DKIM key (admin)
  /api/admin/test/key-material/dkim/upload:
    post:
      operationId: post_api_admin_test_key-material_dkim_upload
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Upload DKIM key (admin)
  /api/admin/test/key-material/relay/generate:
    post:
      operationId: post_api_admin_test_key-material_relay_generate
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Generate relay key (admin)
  /api/admin/test/keyserver-probe:
    post:
      operationId: post_api_admin_test_keyserver-probe
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Keyserver probe (admin)
  /api/admin/test/preview:
    post:
      operationId: post_api_admin_test_preview
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Test send preview (admin)
  /api/admin/test/privacy-posture:
    get:
      operationId: get_api_admin_test_privacy-posture
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Privacy posture report (admin)
  /api/admin/test/readiness:
    get:
      operationId: get_api_admin_test_readiness
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Readiness probe JSON (admin)
  /api/admin/test/send:
    post:
      operationId: post_api_admin_test_send
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Test send (admin)
  /api/admin/test/uploads:
    post:
      operationId: post_api_admin_test_uploads
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Test blob upload (admin)
  /api/admin/test/wrap-roundtrip:
    post:
      operationId: post_api_admin_test_wrap-roundtrip
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Wrap crypto roundtrip (admin)
  /api/admin/uptime:
    get:
      operationId: get_api_admin_uptime
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/admin/uptime
    post:
      operationId: post_api_admin_uptime
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/admin/uptime
  /api/admin/uptime/runs:
    delete:
      operationId: delete_api_admin_uptime_runs
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: DELETE /api/admin/uptime/runs
  /api/admin/uptime/test-probe:
    post:
      operationId: post_api_admin_uptime_test-probe
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/admin/uptime/test-probe
  /api/admin/users:
    get:
      operationId: get_api_admin_users
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: List users (admin)
  /api/admin/users/{userId}:
    delete:
      operationId: delete_api_admin_users_userId
      parameters:
        - in: path
          name: userId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Delete user (admin; full purge)
    get:
      operationId: get_api_admin_users_userId
      parameters:
        - in: path
          name: userId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Get user (admin)
    patch:
      operationId: patch_api_admin_users_userId
      parameters:
        - in: path
          name: userId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Patch user (admin; is_admin)
  /api/admin/users/{userId}/disable:
    post:
      operationId: post_api_admin_users_userId_disable
      parameters:
        - in: path
          name: userId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Disable user (admin)
  /api/admin/users/{userId}/enable:
    post:
      operationId: post_api_admin_users_userId_enable
      parameters:
        - in: path
          name: userId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Re-enable user (admin; SRP accounts only)
  /api/auth/2fa:
    get:
      operationId: get_api_auth_2fa
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: MFA status
  /api/auth/2fa/login/recovery:
    post:
      operationId: post_api_auth_2fa_login_recovery
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Complete login with recovery code
  /api/auth/2fa/login/totp:
    post:
      operationId: post_api_auth_2fa_login_totp
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Complete login with TOTP
  /api/auth/2fa/login/webauthn/begin:
    post:
      operationId: post_api_auth_2fa_login_webauthn_begin
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Begin WebAuthn login step-up
  /api/auth/2fa/login/webauthn/finish:
    post:
      operationId: post_api_auth_2fa_login_webauthn_finish
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Finish WebAuthn login step-up
  /api/auth/2fa/recovery/regenerate:
    post:
      operationId: post_api_auth_2fa_recovery_regenerate
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Regenerate recovery codes
  /api/auth/2fa/totp/{totpId}:
    delete:
      operationId: delete_api_auth_2fa_totp_totpId
      parameters:
        - in: path
          name: totpId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Delete enrolled TOTP factor
  /api/auth/2fa/totp/begin:
    post:
      operationId: post_api_auth_2fa_totp_begin
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Begin TOTP enrollment
  /api/auth/2fa/totp/confirm:
    post:
      operationId: post_api_auth_2fa_totp_confirm
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Confirm TOTP enrollment
  /api/auth/2fa/verify/recovery:
    post:
      operationId: post_api_auth_2fa_verify_recovery
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Verify recovery code
  /api/auth/2fa/verify/totp:
    post:
      operationId: post_api_auth_2fa_verify_totp
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Verify TOTP during sensitive change
  /api/auth/2fa/verify/webauthn/begin:
    post:
      operationId: post_api_auth_2fa_verify_webauthn_begin
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Begin WebAuthn verification for sensitive change
  /api/auth/2fa/verify/webauthn/finish:
    post:
      operationId: post_api_auth_2fa_verify_webauthn_finish
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Finish WebAuthn verification for sensitive change
  /api/auth/2fa/webauthn/{credentialId}:
    delete:
      operationId: delete_api_auth_2fa_webauthn_credentialId
      parameters:
        - in: path
          name: credentialId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Delete WebAuthn credential
  /api/auth/2fa/webauthn/register/begin:
    post:
      operationId: post_api_auth_2fa_webauthn_register_begin
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Begin WebAuthn registration
  /api/auth/2fa/webauthn/register/finish:
    post:
      operationId: post_api_auth_2fa_webauthn_register_finish
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Finish WebAuthn registration
  /api/auth/account-delete/begin:
    post:
      operationId: post_api_auth_account-delete_begin
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/account-delete/begin
  /api/auth/account-delete/cancel:
    post:
      operationId: post_api_auth_account-delete_cancel
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/account-delete/cancel
  /api/auth/account-delete/finish:
    post:
      operationId: post_api_auth_account-delete_finish
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/account-delete/finish
  /api/auth/account-delete/now:
    post:
      operationId: post_api_auth_account-delete_now
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/account-delete/now
  /api/auth/account-delete/schedule:
    post:
      operationId: post_api_auth_account-delete_schedule
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/account-delete/schedule
  /api/auth/appearance:
    patch:
      operationId: patch_api_auth_appearance
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: PATCH /api/auth/appearance
    put:
      operationId: put_api_auth_appearance
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: PUT /api/auth/appearance
  /api/auth/login:
    post:
      operationId: post_api_auth_login
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/login
  /api/auth/login/begin:
    post:
      operationId: post_api_auth_login_begin
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/login/begin
  /api/auth/login/finish:
    post:
      operationId: post_api_auth_login_finish
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/login/finish
  /api/auth/logout:
    post:
      operationId: post_api_auth_logout
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/logout
  /api/auth/me:
    get:
      operationId: get_api_auth_me
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/auth/me
  /api/auth/password:
    post:
      operationId: post_api_auth_password
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/password
  /api/auth/password/begin:
    post:
      operationId: post_api_auth_password_begin
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/password/begin
  /api/auth/password/finish:
    post:
      operationId: post_api_auth_password_finish
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/password/finish
  /api/auth/register:
    post:
      operationId: post_api_auth_register
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/auth/register
  /api/auth/signup-config:
    get:
      operationId: get_api_auth_signup-config
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/auth/signup-config
  /api/bootstrap.json:
    get:
      operationId: get_api_bootstrap.json
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/bootstrap.json
  /api/migrate/posts:
    post:
      operationId: post_api_migrate_posts
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/migrate/posts
  /api/pgp/keys:
    post:
      operationId: post_api_pgp_keys
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/pgp/keys
  /api/posts:
    get:
      operationId: get_api_posts
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/posts
    post:
      operationId: post_api_posts
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/posts
  /api/site/topbar.json:
    get:
      operationId: get_api_site_topbar.json
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/site/topbar.json
  /api/telemetry/browser:
    post:
      operationId: post_api_telemetry_browser
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: POST /api/telemetry/browser
  /api/tools/calls:
    get:
      operationId: get_api_tools_calls
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/tools/calls
  /api/uptime.json:
    get:
      operationId: get_api_uptime.json
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/uptime.json
  /api/v1/account:
    delete:
      operationId: delete_api_v1_account
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: DELETE /api/v1/account
  /api/v1/account-key/bootstrap:
    post:
      operationId: post_api_v1_account-key_bootstrap
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Upload account key bootstrap
  /api/v1/account-key/me:
    get:
      operationId: get_api_v1_account-key_me
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Wrapped account secret for current user
  /api/v1/account/delete-policy:
    get:
      operationId: get_api_v1_account_delete-policy
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/v1/account/delete-policy
    put:
      operationId: put_api_v1_account_delete-policy
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: PUT /api/v1/account/delete-policy
  /api/v1/billing/status:
    get:
      operationId: get_api_v1_billing_status
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: GET /api/v1/billing/status
  /api/v1/custom-domains:
    get:
      operationId: get_api_v1_custom-domains
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: List owned custom domains
    post:
      operationId: post_api_v1_custom-domains
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Add custom domain
  /api/v1/custom-domains/{domain}:
    delete:
      operationId: delete_api_v1_custom-domains_domain
      parameters:
        - in: path
          name: domain
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Remove custom domain
  /api/v1/custom-domains/{domain}/catchall:
    put:
      operationId: put_api_v1_custom-domains_domain_catchall
      parameters:
        - in: path
          name: domain
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Set catch-all identity
  /api/v1/custom-domains/{domain}/verify:
    post:
      operationId: post_api_v1_custom-domains_domain_verify
      parameters:
        - in: path
          name: domain
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Verify custom domain
  /api/v1/filters:
    get:
      operationId: get_api_v1_filters
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: List mail filters
    post:
      operationId: post_api_v1_filters
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Create mail filter
  /api/v1/filters/{filterId}:
    delete:
      operationId: delete_api_v1_filters_filterId
      parameters:
        - in: path
          name: filterId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Delete mail filter
    put:
      operationId: put_api_v1_filters_filterId
      parameters:
        - in: path
          name: filterId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Update mail filter
  /api/v1/identities:
    get:
      operationId: get_api_v1_identities
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: List identities
    post:
      operationId: post_api_v1_identities
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Add identity
  /api/v1/identities/{fingerprint}:
    delete:
      operationId: delete_api_v1_identities_fingerprint
      parameters:
        - in: path
          name: fingerprint
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Hard-delete identity
    patch:
      operationId: patch_api_v1_identities_fingerprint
      parameters:
        - in: path
          name: fingerprint
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Patch identity metadata
  /api/v1/identities/{fingerprint}/default:
    post:
      operationId: post_api_v1_identities_fingerprint_default
      parameters:
        - in: path
          name: fingerprint
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Set default identity
  /api/v1/identities/{fingerprint}/revoke:
    post:
      operationId: post_api_v1_identities_fingerprint_revoke
      parameters:
        - in: path
          name: fingerprint
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Revoke identity
  /api/v1/identities/visible-profile:
    get:
      operationId: get_api_v1_identities_visible-profile
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Public-visible identity profile
  /api/v1/keys/contacts:
    get:
      operationId: get_api_v1_keys_contacts
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: List saved contact keys
    post:
      operationId: post_api_v1_keys_contacts
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Upsert contact key
  /api/v1/keys/contacts/{email}:
    delete:
      operationId: delete_api_v1_keys_contacts_email
      parameters:
        - in: path
          name: email
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Delete contact keys for address
    get:
      operationId: get_api_v1_keys_contacts_email
      parameters:
        - in: path
          name: email
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Get contact key for address
  /api/v1/keys/lookup:
    get:
      operationId: get_api_v1_keys_lookup
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Resolver chain key lookup
  /api/v1/mail/messages:
    get:
      operationId: get_api_v1_mail_messages
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: List message manifests
    post:
      operationId: post_api_v1_mail_messages
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Ingest internal ciphertext message
  /api/v1/mail/messages/{messageId}:
    delete:
      operationId: delete_api_v1_mail_messages_messageId
      parameters:
        - in: path
          name: messageId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Delete message
    get:
      operationId: get_api_v1_mail_messages_messageId
      parameters:
        - in: path
          name: messageId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Get message manifest
    patch:
      operationId: patch_api_v1_mail_messages_messageId
      parameters:
        - in: path
          name: messageId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Patch manifest metadata
  /api/v1/mail/messages/{messageId}/blob:
    get:
      operationId: get_api_v1_mail_messages_messageId_blob
      parameters:
        - in: path
          name: messageId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Stream body ciphertext
  /api/v1/mail/outbox:
    post:
      operationId: post_api_v1_mail_outbox
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Queue OpenPGP outbox payload
  /api/v1/mail/outbox-plain:
    post:
      operationId: post_api_v1_mail_outbox-plain
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Plaintext relay (disabled for user mail; returns 410)
  /api/v1/mail/outbox/{outboxId}:
    get:
      operationId: get_api_v1_mail_outbox_outboxId
      parameters:
        - in: path
          name: outboxId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Outbox status
  /api/v1/mail/protected-links:
    post:
      operationId: post_api_v1_mail_protected-links
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Create protected-link message
  /api/v1/mail/search/metadata:
    get:
      operationId: get_api_v1_mail_search_metadata
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Server-side metadata search (consented fields only)
  /api/v1/mail/sender-icon:
    get:
      operationId: get_api_v1_mail_sender-icon
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Resolve BIMI or favicon for domain
  /api/v1/mail/settings:
    get:
      operationId: get_api_v1_mail_settings
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Mail settings and retention
    post:
      operationId: post_api_v1_mail_settings
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Update mail settings
    put:
      operationId: put_api_v1_mail_settings
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Update mail settings (alias)
  /api/v1/mail/test/echo:
    post:
      operationId: post_api_v1_mail_test_echo
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Self-deliver ciphertext selfcheck
  /api/v1/mail/usable-domains:
    get:
      operationId: get_api_v1_mail_usable-domains
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Domains usable for new mail identities
  /api/v1/mailbox/folders:
    get:
      operationId: get_api_v1_mailbox_folders
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: List mailbox folders
    post:
      operationId: post_api_v1_mailbox_folders
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Create custom folder
  /api/v1/mailbox/folders/{folderName}:
    delete:
      operationId: delete_api_v1_mailbox_folders_folderName
      parameters:
        - in: path
          name: folderName
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Delete custom folder
  /api/v1/protected-links/{token}/meta:
    get:
      operationId: get_api_v1_protected-links_token_meta
      parameters:
        - in: path
          name: token
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Protected link KDF metadata (public, rate-limited)
  /api/v1/protected-links/{token}/open:
    post:
      operationId: post_api_v1_protected-links_token_open
      parameters:
        - in: path
          name: token
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Consume view and return ciphertext (public, rate-limited)
  /api/v1/smtp-submission:
    get:
      operationId: get_api_v1_smtp-submission
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: List SMTP credentials
    post:
      operationId: post_api_v1_smtp-submission
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Create SMTP credential
  /api/v1/smtp-submission/{credentialId}:
    delete:
      operationId: delete_api_v1_smtp-submission_credentialId
      parameters:
        - in: path
          name: credentialId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Delete SMTP credential
  /api/v1/smtp-submission/{credentialId}/regenerate:
    post:
      operationId: post_api_v1_smtp-submission_credentialId_regenerate
      parameters:
        - in: path
          name: credentialId
          required: true
          schema:
            type: string
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
          description: Response (JSON or error body; see handler and docs/e2ee-mail-spec.md for mail).
      summary: Regenerate SMTP password
servers:
  - url: /
