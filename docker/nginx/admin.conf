server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location = /healthz {
        default_type application/json;
        return 200 '{"ok":true,"surface":"admin"}';
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
